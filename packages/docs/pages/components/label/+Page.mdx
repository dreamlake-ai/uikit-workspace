---
title: Label
section: Components
order: 255
description: Form label with size variants, associated via htmlFor.
---

import { Preview } from '../../../components/Preview'
import { SizesSpec } from '../../../components/specimens/components/label/SizesSpec'
import sizesSrc from '../../../components/specimens/components/label/SizesSpec.tsx?raw'

# Label

A text label for form controls. Render a native `<label htmlFor="…">` (or wrap
the control) so clicks focus the input. `size` picks the type scale; `hint` also
mutes the color for helper text.

<Preview source={sizesSrc} filename="SizesSpec.tsx">
  <SizesSpec />
</Preview>

## Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `size` | `'xs' \| 'sm' \| 'lg' \| 'hint'` | `'sm'` | Type scale (`hint` is muted). |
| `className` | `string` | — | Extra classes. |

All native `<label>` attributes (`htmlFor`, …) are forwarded.
