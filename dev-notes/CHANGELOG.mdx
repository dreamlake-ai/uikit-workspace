# dev-notes changelog

Release notes for engineering docs in `dev-notes/`. Each entry covers
substantive doc changes — new patterns, removed advice, restructured
sections. README copy edits and typo fixes are skipped. See
[`./README.mdx`](./README.mdx) for the folder convention and
[`../CLAUDE.md`](../CLAUDE.md) for the release-notes policy.

## 2026-05-09 — Foundations / Typography

- Lands `/foundations/typography` — port of
  `staging/dreamlake-design-guide.html` §03. Single-page (no `##`
  sections to split into files), Lede + `<TypeSpecimen />` + a
  short note on the global `ss01` / `cv11` Inter Tight feature
  settings.
- New `<TypeSpecimen />` component renders the 9 typographic
  classes (h1 / h2 / h3 / body / row · ledger / meta · timestamp /
  eyebrow / tag / kbd) live, each next to its spec annotation
  (font + size/leading + tracking). Demo strings ported verbatim
  from the design-guide HTML.
- `src/data/nav.ts` flips typography todo → live. No `toc.ts`
  entry needed (page has no h2 anchors).

## 2026-05-09 — Foundations / Semantic palette

- Lands `/foundations/palette` per the per-section-file pattern from
  the Surfaces PR. Sections: overview (Lede + `<PaletteGrid />`),
  rule ("When adding a new concept", 4 numbered rules), why ("Why six
  and not more", 3 bullets).
- New `<PaletteGrid />` component renders the 6 buckets from a data
  array. Hex values inline; per-domain concept tags ported verbatim
  from `staging/dreamlake-design-guide.html` §02 and `staging/design.md`.
- `src/data/nav.ts` flips palette todo → live; `src/data/toc.ts`
  registers the new route's TOC.

## 2026-05-09 — Phase 3 begins: Foundations / Surfaces

- First content-bearing design-guide page lands at `/foundations/surfaces`.
  Establishes the page-shape pattern for the rest of Phase 3:
  - One MDX file per `##` section under `pages/<route>/sections/`.
  - `+Page.mdx` composes the sections via default-export imports.
  - Live React demos under `src/components/design/` (e.g.
    `SurfaceDemo`, `SurfaceTokens`) embedded in the relevant section.
- LeftNav reorganized from the legacy ros-viz groups (COMPONENTS /
  EXAMPLES / CONCEPTS / HOOKS — all placeholder anchors) to the
  design-guide structure (FOUNDATIONS / GEOMETRY / ZEBRA LISTS /
  COMPONENTS · ATOMS / COMPONENTS · CHROME / COMPONENTS · LISTS /
  REFERENCE). Subsequent Phase 3 PRs flip `todo: true` items to
  live as their pages land.
- Right-rail TOC is now per-route via `src/data/toc.ts` (a
  route → `TocItem[]` map), looked up in `pages/+Layout.tsx` via
  Vike's `usePageContext().urlPathname`. New pages add their entry
  here. Resolves the long-standing TODO at the top of `+Layout.tsx`.

## 2026-05-09 — Sync from viz-workspace: Prose-as-MDX + shiki

- Pulled the *Prose as MDX, components as imports* section into
  `migration-patterns.mdx` from `dreamlake-ai/viz-workspace`. Adds the
  one-time MDX wiring (compiler + components map + wrapper), the
  components-map vs inline-import decision rule, the second/third-page
  tipping-point heuristic, and the "pin the TOC to slugs, not magic
  strings" warning.
- Pulled the *Land syntax highlighting in the same milestone* sub-
  section: the three-knob shiki wiring (defaultColor:false +
  parseMetaString + custom data-language transformer) that lets fenced
  code blocks slot into `<CodeBlock>` chrome without the page knowing
  shiki exists.
- Updated step 4 of the *Small reviewable PRs* sequence and added
  step 8 (MDX conversion) so the order matches what the upstream now
  recommends.
- This repo's matching infrastructure landed in PR — same shiki + MDX
  conversion, applied to our quick-start placeholder page (still
  ros-viz prose at the time of writing, will be replaced wholesale by
  the design-guide migration in Phase 3).

## 2026-05-09 — Adopt PR workflow + changelog policy

- Pulled `CLAUDE.md` (PR-based workflow, no direct pushes to `main`)
  and the changelog policy from `dreamlake-ai/viz-workspace`. This
  repo inherits the same conventions verbatim — same stack, same
  shape of doc, same agents working on both.
- Created this changelog. From here on, every substantive
  `dev-notes/*.mdx` update lands a release note here, and the PR
  description cites the entry.
- Updated `README.mdx` to reference both the PR workflow and this
  changelog.

Direct pushes to `main` during the initial bring-up of this repo
(fork from viz-workspace, identity rebrand, dev-notes pull) were
intentional — move fast while shaping the surface. From this PR
onward: branch + PR for everything that lands on `main`.

## 2026-05-09 — Initial dev-notes set (forked from viz-workspace)

- Pulled `migration-patterns.mdx` and `README.mdx` verbatim from
  `dreamlake-ai/viz-workspace`. The patterns are stack-specific
  (Vike + React 19 + TW4 + pnpm + Netlify) and apply 1:1 here.
- No project-identifying references to scrub — content is generic
  enough to share across both repos.
