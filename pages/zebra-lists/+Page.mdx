import { Crumbs, Lede } from '../../src/components/prose'
import FourBandModel from './sections/four-band-model.mdx'
import States from './sections/states.mdx'
import MultiRow from './sections/multi-row.mdx'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }]} here="Zebra lists" />

# Zebra lists — the four-band model

<Lede>
  A zebra list is a tight, mono-friendly, single-line list with no per-row borders, no per-row
  shadows, no per-row chrome — all visual state lives on the row's `background`. Rest rhythm
  is carried by alternating fills; interaction is carried by hue.
</Lede>

<FourBandModel />

<States />

<MultiRow />
