import { Lede } from '../../../../src/components/prose'
import { SurfaceDemo } from '../../../../src/components/design/SurfaceDemo'

<Lede>
  Every page loads `tokens.css` as its sole document-level color source. The surface stack
  has three tokens — `--bg`, `--panel-bg`, `--rail-bg` — and they relate differently in
  light vs. dark.
</Lede>

<SurfaceDemo />
