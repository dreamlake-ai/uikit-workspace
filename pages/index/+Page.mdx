import { Callout, Crumbs, In, Lede } from '../../src/components/prose'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }]} here="Overview" />

# DreamLake UIkit

<Lede>
  Single source of truth for surfaces, ink, semantic color, type, geometry, and the
  zebra-list patterns shared by every DreamLake page — `ml-dash`, `dreamlake`,
  `dreamlake-pipelines`, `dreamlake-profile`, `dreamlake-timetravel`, `notebook`.
</Lede>

These docs document a real React component library: tokens that paint surfaces, six
hues that color domain concepts, two typefaces that split identity from narrative, and
a small kit of components (chip, button, zebra row, status indicator, …) every
DreamLake app already uses.

## Install

```bash file="terminal"
npm install @dreamlake/uikit
# or
pnpm add @dreamlake/uikit
```

## Foundations

The token contract that every page loads first. Read these in order — each builds on
the previous.

- <In href="/foundations/surfaces">Surfaces — page · panel · rail</In> — the 3-tier
  surface stack and why the tiers invert in dark mode.
- <In href="/foundations/palette">Semantic palette — six hues, no seventh</In> — one
  shared 6-color set for every domain concept across every page.
- <In href="/foundations/typography">Typography — Inter Tight + JetBrains Mono</In> —
  mono carries identity, UI carries narrative; nine specimens live.

## Components

Reusable React primitives. The shared vocabulary every DreamLake app builds on top of.

- <In href="/components/chip">Chip</In> — small status-tinted label.
- <In href="/components/button">Buttons</In> — primary, secondary, ghost,
  destructive.
- More atoms (status indicator, kbd, avatar, dropdown, timeline) and the chrome /
  list components are landing one at a time. Check the left rail for what's live;
  faded items are still in flight.

<Callout type="info">
  **Reading the source.** Every doc page cites its design-guide section
  (`staging/dreamlake-design-guide.html`) and any cross-referenced reasoning
  (`staging/design.md`, `staging/zebra-list-style-guide.md`). Code examples come
  straight from the components in this repo — what you see is what you ship.
</Callout>
