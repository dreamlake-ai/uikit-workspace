import { Crumbs, Lede } from '../../../src/components/prose'
import Anatomy from './sections/anatomy.mdx'
import States from './sections/states.mdx'
import StatusBuckets from './sections/status-buckets.mdx'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }, { href: '/components', label: 'Components' }]} here="Zebra row" />

# Zebra row — the foundational selectable list row · base · zebra · hover · selected

<Lede>
  The atom every list panel in the guide stacks. A 4-column mono grid (id ·
  label · meta · status) on a 7px-radius surface that switches between base and
  zebra parity from the array index, lifts to a hover band on pointer, and
  swells to a 10px-radius accent fill when selected. The reusable
  `<ZebraRow />` from `@dreamlake/uikit` — distinct from the foundations
  `/zebra-lists` page, which documents the *pattern* this atom implements.
</Lede>

<Anatomy />

<States />

<StatusBuckets />
