import { Crumbs, Lede } from '../../../src/components/prose'
import Buckets from './sections/buckets.mdx'
import Styles from './sections/styles.mdx'
import Usage from './sections/usage.mdx'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }, { href: '/components', label: 'Components' }]} here="Status indicator" />

# Status indicator — bucket-coloured dot · mono label · optional secondary

<Lede>
  A 1.0 dot in the bucket hue, a mono label, and an optional muted secondary
  clause. Six buckets pulled from the cross-page semantic palette; three style
  variants (`dot`, `dot-pulse`, `bar`) for steady state, live state, and
  inline-tag use. The hexes stay inline at every use site — there is no `--ok`
  or `--error` token.
</Lede>

<Buckets />

<Styles />

<Usage />
