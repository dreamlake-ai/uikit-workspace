import { Lede } from '../../../../src/components/prose'

<Lede>
  A small mono pill. Carries one short attribute — a kind, a status, a filter, a
  breadcrumb segment. Mono 10.5px / 500 / +0.04em, padded to 4×10, radius 8. The
  default chip is neutral; the five tinted tones map onto the cross-page
  semantic palette.
</Lede>
