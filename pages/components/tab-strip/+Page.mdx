import { Crumbs, Lede } from '../../../src/components/prose'
import Variants from './sections/variants.mdx'
import States from './sections/states.mdx'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }, { href: '/components', label: 'Components' }]} here="Tab strip" />

# Tab strip — mono caps + 2px ink underline

<Lede>
  Mono small-caps tabs with an animated 2px ink underline (FLIP-style). The
  active tab promotes to ink; every other tab stays muted. Optional trailing
  count rides each label at .55 opacity. Same atom every dream.lake product
  reuses for view-mode swaps and inspector panes.
</Lede>

<Variants />

<States />
