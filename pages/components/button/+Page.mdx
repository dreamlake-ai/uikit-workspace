import { Crumbs, Lede } from '../../../src/components/prose'
import Variants from './sections/variants.mdx'
import States from './sections/states.mdx'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }, { href: '/components', label: 'Components' }]} here="Buttons" />

# Buttons — ghost & primary

<Lede>
  Two variants. **Ghost** is the default action surface — muted ink, faint hairline,
  transparent fill. **Primary** is the accent-fill CTA, one per surface. Mono 10.5px caps,
  6px radius, 1px border. The same two are the only buttons every dream.lake product reuses.
</Lede>

<Variants />

<States />
