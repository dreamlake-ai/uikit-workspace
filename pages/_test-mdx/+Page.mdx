# MDX smoke test

Plain markdown body, ports through the prose wrapper. Delete this route
once Phase 3 starts landing real MDX pages.

## Heading two with auto-id

The H2 above should render as the prose `H2` component (sticky, anchored)
with its `id` derived by `rehype-slug` from the heading text.

### Heading three

A paragraph with `inline code` and **bold** and *italic*.

- bullet one
- bullet two
- bullet three

| col a | col b |
|---|---|
| `cell` | text |
