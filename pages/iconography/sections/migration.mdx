import { MigrationBacklog } from '../../../src/components/design/IconCatalog'

## Migration backlog

A handful of legacy custom SVGs still ship inline in `wireframe-shared.jsx` and
`timetravel-view.jsx`. They _read_ like Lucide but aren't — replace each with the
canonical name on next touch.

<MigrationBacklog />
