import { Crumbs, Lede } from '../../src/components/prose'
import Categories from './sections/categories.mdx'
import Migration from './sections/migration.mdx'

<Crumbs trail={[{ href: '/', label: '@dreamlake/uikit' }]} here="Iconography" />

# Iconography — every glyph, all from Lucide

<Lede>
  Every icon across `dreamlake`, `pipelines`, `timetravel`, `profile`, and `notebook` is
  sourced from [lucide.dev](https://lucide.dev) v0.460. Render via `lucide-react` so the
  path data matches upstream byte-for-byte — no hand-copied SVG. Stroke 1.4–1.6 at small
  sizes (12–16px); 1.1–1.25 at large (24–28px).
</Lede>

<Categories />

<Migration />
