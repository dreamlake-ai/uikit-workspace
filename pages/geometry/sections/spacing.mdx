import { SpacingScale } from '../../../src/components/design/SpacingScale'

## Spacing scale

Eleven steps from 2 to 40px. There's no token — values are used inline. Most padding lives
between 6–18px; the 2px row gap is sacred. The big jumps at the top (20 → 28 → 40) are for
section breathing room, not row rhythm.

<SpacingScale />
