# PR screenshots

Before/after UI captures referenced from pull-request descriptions.

Orphan branch — no shared history with `main`, nothing here ships in the
package. Each image is a side-by-side of the same docs specimen rendered off
`main` and off the PR branch: same viewport, same clip, 2× DPR, both themes.
